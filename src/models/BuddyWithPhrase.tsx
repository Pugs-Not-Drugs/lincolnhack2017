export class BuddyWithPhrase {
	phrase: string;
	image: string;
	bannerClass: string

	constructor(phrase: string, image: string, bannerClass: string) {
		this.phrase = phrase;
		this.image = image;
		this.bannerClass = bannerClass;
	}
}